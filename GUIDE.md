# 초보자용 따라하기 가이드 (윈도우)

대전 선상주차장 정기주차권을 **2026-07-21 0시**에 자동 신청하기.
아래를 위에서부터 순서대로 그대로 따라 하세요.

---

## STEP 0. Node.js 설치 (딱 한 번)

1. 브라우저에서 https://nodejs.org 접속
2. **초록색 LTS 버튼** 클릭 → 설치 파일 다운로드 → 실행
3. 설치 창에서 계속 **Next** (전부 기본값) → 마지막에 **Finish**

> 이미 설치돼 있다면 이 단계는 건너뛰어도 됩니다.

---

## STEP 1. 프로그램 다운로드

1. 브라우저 주소창에 아래를 붙여넣고 접속 → ZIP 파일이 다운로드됩니다.

   ```
   https://github.com/swap08/opentutorials-swap08/archive/refs/heads/claude/ticket-booking-automation-xuzzfv.zip
   ```

2. 다운로드된 ZIP 파일을 **바탕화면**으로 옮깁니다.

---

## STEP 2. 압축 풀기

1. ZIP 파일 위에서 **오른쪽 클릭 → "압축 풀기(Extract All)"** → **압축 풀기** 버튼
2. 새로 생긴 폴더를 엽니다. 폴더 이름이 길어요:
   `opentutorials-swap08-claude-ticket-booking-automation-xuzzfv`
3. 그 안에 **`package.json`, `src` 폴더** 가 보이면 제대로 찾은 겁니다.
   (폴더 안에 폴더가 하나 더 있으면, `package.json` 이 보이는 위치까지 들어가세요.)

---

## STEP 3. 그 폴더에서 터미널(PowerShell) 열기

1. `package.json` 이 보이는 폴더를 파일 탐색기로 연 상태에서,
2. 위쪽 **주소창을 마우스로 클릭** (경로가 파랗게 선택됨)
3. 거기에 `powershell` 이라고 타이핑하고 **Enter**
4. **파란색 창(PowerShell)** 이 그 폴더 위치에서 열립니다. ← 앞으로 명령은 전부 여기에 입력

> ✅ 확인법: 파란 창 맨 앞 경로가 `...ticket-booking-automation-xuzzfv>` 로 끝나면 맞습니다.

---

## STEP 4. 설치 (한 번, 각각 1~2분 걸림)

파란 창에 아래를 **한 줄씩** 붙여넣고 Enter. 끝날 때까지 기다린 뒤 다음 줄.

```
npm install
```
```
npx playwright install chromium
```

> ⏳ 글자가 주르륵 나오다가 멈추고 다시 입력할 수 있게 되면 완료된 겁니다.
> ❗ "npm 을(를) 인식할 수 없습니다" 가 나오면 → STEP 0(Node.js 설치)이 안 된 것. 설치 후 창을 껐다 다시 여세요.

---

## STEP 5. 설정 파일 만들기

파란 창에 그대로:

```
copy config.example.json config.json
```

이러면 `config.json` 파일이 생깁니다. (이 파일에 목표 시각·주차장 정보가 이미 채워져 있어요.)

---

## STEP 6. 로그인 (한 번, 휴대폰 인증 포함)

파란 창에:

```
npm run login
```

1. **브라우저 창이 자동으로 열립니다.**
2. 그 창에서 KNPARK 에 **직접 로그인** 하세요. (아이디/비밀번호 + 휴대폰 본인인증)
3. 로그인이 다 끝나면 **파란 창(터미널)으로 돌아와서 Enter** 를 누릅니다.
4. "완료!" 메시지가 나오면 로그인 상태가 저장된 겁니다. (다음부터는 로그인 안 해도 됨)

---

## STEP 7. 신청 페이지 주소 확인 → 설정에 입력

대전 선상주차장 신청 페이지 주소를 알아내서 설정에 넣는 단계입니다.

1. 파란 창에:

   ```
   npm run inspect
   ```

2. 브라우저가 열리면, **로그인된 상태로** KNPARK 에서
   **대전 선상주차장 정기주차권 신청 페이지까지 직접 이동**하세요.
3. 그 페이지의 **주소창 주소(URL)를 복사**합니다. (예: `https://www.knpark.com/...`)
4. 확인이 끝나면 브라우저를 닫습니다. (파란 창에서 Ctrl+C 로 종료해도 됩니다)
5. 폴더에서 **`config.json`** 파일을 **메모장**으로 엽니다.
   (파일 오른쪽 클릭 → 연결 프로그램 → 메모장)
6. `"reservationUrl": "https://www.knpark.com/",` 부분에서 주소를 **복사한 주소로 바꾸고 저장**합니다.
   (`Ctrl+S` 로 저장)

> 💡 이 부분이 어려우면, 3번에서 복사한 주소를 저(클로드)에게 붙여주세요. 제가 대신 넣어드립니다.

---

## STEP 8. 예매 실행 (로그인도 여기서 함)

**예약일 20일 밤 23:40쯤** (여유 있게) 파란 창을 열고(STEP 3 방식), 아래를 입력:

```
npm run book
```

그러면 프로그램이:

1. KNPARK 서버시간에 맞춥니다.
2. 브라우저를 열고 신청 페이지로 이동합니다.
3. **로그인이 안 돼 있으면 "지금 로그인하세요" 라고 안내가 나옵니다.**
   → 그 브라우저 창에서 **직접 로그인**(아이디/비번 + 휴대폰 본인인증)하세요.
   → 로그인이 확인되면 프로그램이 자동으로 0시까지 대기합니다. (중간중간 새로고침하며 로그인 유지)
4. **21일 0시 정각에 '신청' 버튼을 자동으로 누릅니다.** (실패해도 계속 재시도)
5. 캡차나 결제가 뜨면 **그 창에서 직접** 처리하세요.
6. 다 끝나면 파란 창에서 **Enter** 를 눌러 종료합니다.

> ⚠️ 로그인을 마친 뒤 0시까지 **PC를 켜두고, 파란 창과 브라우저를 닫지 마세요.**
> 💡 로그인은 이 STEP 8 안에서 하므로, 앞의 STEP 6(`npm run login`)은 안 해도 됩니다.
>    (STEP 6은 미리 로그인/셀렉터 확인을 해보고 싶을 때만 쓰는 보조 기능이에요.)

---

## 자주 나오는 문제

| 증상 | 해결 |
|------|------|
| `npm 을(를) 인식할 수 없습니다` | Node.js 미설치. STEP 0 후 창을 껐다 다시 열기 |
| `config.json 이 없습니다` | STEP 5 (copy 명령)를 안 한 것 |
| 로그인 상태가 아니라고 나옴 | STEP 6(`npm run login`)을 다시 실행 |
| 신청 버튼을 못 누름 | 신청 페이지 주소(STEP 7)가 정확한지 확인 |
| 창이 안 뜸 / 멈춤 | 파란 창의 빨간 글자(오류)를 복사해서 클로드에게 보여주기 |

---

## 한눈에 보는 전체 순서

```
[한 번만]
STEP 0  Node.js 설치
STEP 1  ZIP 다운로드
STEP 2  압축 풀기
STEP 3  폴더에서 PowerShell 열기
STEP 4  npm install  /  npx playwright install chromium
STEP 5  copy config.example.json config.json
STEP 6  npm run login   → 브라우저에서 직접 로그인(휴대폰 인증)
STEP 7  npm run inspect → 신청 페이지 주소 확인 → config.json 에 입력

[예약 당일]
STEP 8  20일 23:40  →  npm run book  →  (창에서 로그인) →  0시 자동 신청
```

> 참고: 로그인은 STEP 8 안에서 하므로, STEP 6(npm run login)을 건너뛰어도 됩니다.
> STEP 7(신청 페이지 주소 확인)만 미리 해두면 STEP 8 한 번으로 끝납니다.
